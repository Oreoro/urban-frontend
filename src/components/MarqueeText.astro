---
interface Props {
  text: string;
  tag?: string;
}

const { text, tag } = Astro.props;
---

<section class="marquee-section" aria-label="Tagline">
  <div class="marquee-inner">
    <p class="marquee-text">{text}</p>
  </div>
  {tag && <p class="marquee-tag">{tag}</p>}
</section>
